name: "gcc-base"
category: "system"
version: 15.3.0+2
uri:
  - https://gcc.gnu.org
license: "GPL-3.0-or-later WITH GCC-exception-3.1 AND GFDL-1.3-or-later"
description: "The core GCC runtime libraries (such as libgcc and libstdc++) required to run compiled C and C++ applications on the system"
gcc_version: "15"
requires:
  - category: "mocaccino"
    name: "cli"
    version: ">=0"
labels:
  llvm_slot: "22"
