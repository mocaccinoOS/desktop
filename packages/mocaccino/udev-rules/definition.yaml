category: "mocaccino"
name: "udev-rules"
version: "1.4.2"
description: "Essential rules for udev to dynamically manage device nodes in /dev, ensuring proper hardware detection and permission handling."
labels:
requires:
  - category: "layers"
    name: "system-x"
    version: ">=0"
  - category: "mocaccino"
    name: "cli"
    version: ">=0"
provides:
