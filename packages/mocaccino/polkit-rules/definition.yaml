category: "mocaccino"
name: "polkit-rules"
version: "1.1"
description: "Default Polkit (PolicyKit) rules and authorization policies to manage unprivileged processes communicating with privileged processes."
labels:
requires:
  - category: "layers"
    name: "system-x"
    version: ">=0"
  - category: "mocaccino"
    name: "cli"
    version: ">=0"
provides:
