install:
- plymouth-set-default-theme mocaccino
# We do not want to run this during iso creation
# It results in broken dracut on the live image. 
# Needs some investigation
# - mocaccino-dracut --rebuild-all
