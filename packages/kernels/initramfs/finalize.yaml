install:
- "! grep -Eq '(docker|containerd|kubepods|actions|nspawn)' /proc/1/cgroup 2>/dev/null && for kver in /lib/modules/*; do depmod -a \"$(basename \"$kver\")\"; done && mocaccino-dracut --rebuild-all || true"